Systemd-manage Installation
===========================

Quick start
===========
Systemd-manage could be build with qmake for its build system,
thus the basic install procedure can be summarized as:

build with qmake:
  qmake  
  make
  make install
  
run:
  systemd-manage


generate package format
===========
it can also be compiled into distribution software packages.
such as rpm or deb package format, tar package.

  cd dist
  ./build.sh   rpm|deb|tar

